//Player class for making the player
//-start location (pos)
//-Health (hp)
//-Damage (dam)
//-Lives? (lives)

class Player {
  constructor(pos, hp, dam, lives) {
    this.pos = pos;
    this.hp = hp;
    this.dam = dam;
    this.lives = lives;
  }
}
