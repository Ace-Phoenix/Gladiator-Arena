//Player class for making the player
//-start location (pos)
//-Health (hp)
//-Damage (dam)
//-Lives? (lives)

class Player {
  constructor(xPos, yPos, hp, dam, lives) {
    this.xPos = xPos;
    this.yPos = yPos;
    this.hp = hp;
    this.dam = dam;
    this.lives = lives;
  }
}
